{% extends "base.jinja2" %}
{% from "macros/form_helpers.jinja2" import render_field %}

{% block title %}Password Reset - VH7{% endblock %}

{% block content %}
    <section class="section">
        <div class="container">
            <h1 class="title is-2">Password Reset</h1>

            {% include "partials/flash.jinja2" %}

            <form action="{{ url_for('users.forgot_password_reset', token=token) }}" method="post">
                {{ form.csrf_token }}
                {{ render_field(form.new_password, placeholder="New Password") }}
                {{ render_field(form.password_confirm, placeholder="New Password Confirm") }}
                <div class="field">
                    <div class="control">
                        <button type="submit" class="button is-primary">
                            <span class="icon">
                                <i class="fas fa-paper-plane"></i>
                            </span>
                            <span>Update</span>
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </section>
{% endblock %}