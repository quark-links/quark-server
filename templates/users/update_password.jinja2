{% extends "base.jinja2" %}
{% from "macros/form_helpers.jinja2" import render_field %}

{% block title %}Update Password - VH7{% endblock %}

{% block content %}
    <section class="section">
        <div class="container">
            <h1 class="title is-2">Update Password</h1>

            {% include "partials/flash.jinja2" %}

            <form action="{{ url_for('users.account_password') }}" method="post">
                {{ form.csrf_token }}
                {{ render_field(form.current_password, placeholder="Current Password") }}
                {{ render_field(form.new_password, placeholder="New Password") }}
                <div class="field">
                    <div class="control">
                        <button type="submit" class="button is-primary">
                            <span class="icon">
                                <i class="fas fa-paper-plane"></i>
                            </span>
                            <span>Update</span>
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </section>
{% endblock %}